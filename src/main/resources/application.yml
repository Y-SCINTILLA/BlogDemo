server:
  port: 8080

spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/blogdemo?useSSL=false&serverTimezone=UTC
      username: root
      password: lxcswxj276h

mybatis:
  mapper-locations: classpath:mapper/*.xml